# Frontend

The frontend will use React. It will access the backend through one API client area and read the backend base URL from local configuration.

The first implementation slice contains:

- Paper search
- Save paper action
- Saved paper list
- Empty and error states for these actions

Until the matching backend endpoints are ready, use `../docs/mock-data/first-slice.json`. Do not place fixed response data inside page components.

